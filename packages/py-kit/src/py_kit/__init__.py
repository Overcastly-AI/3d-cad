"""Loft shared service kit.

Service bootstrap (pydantic-settings config, structlog JSON logging, FastAPI
app factory with /healthz + /readyz, error envelope, arq queue client) lands
with the `packages/py-kit` backlog item — see docs/BACKLOG.md.
"""

__all__: list[str] = []
