/**
 * @loft/design — design tokens, UI primitives, fonts (source-only, RESEARCH.md §5).
 * Tokens land with the web-shell backlog item; this is the workspace placeholder.
 */
export const DESIGN_PACKAGE_NAME = "@loft/design" as const;
