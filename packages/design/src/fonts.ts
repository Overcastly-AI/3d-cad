/**
 * Self-hosted fonts (@fontsource, OFL-licensed) — import once from the app
 * entry. No CDN font requests, ever (CLAUDE.md quality floor).
 *
 * Faces (see tokens.ts `font`):
 * - Hanken Grotesk — body/UI
 * - Fragment Mono — display (wordmark, eyebrows) + data readouts
 */
import "@fontsource/hanken-grotesk/400.css";
import "@fontsource/hanken-grotesk/500.css";
import "@fontsource/hanken-grotesk/600.css";
import "@fontsource/fragment-mono/400.css";
