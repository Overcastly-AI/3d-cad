[project]
name = "loft-gateway"
version = "0.1.0"
description = "Loft gateway service: auth, REST aggregation, WebSocket fan-out"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "loft-py-kit",
    "fastapi>=0.115",
    "anyio>=4",
    "uvicorn>=0.32",
    "httpx2>=2.5",
    "argon2-cffi>=23.1",
    "pyjwt>=2.9",
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30",
    "alembic>=1.14",
    "email-validator>=2.2",
]

[tool.uv.sources]
loft-py-kit = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gateway"]
