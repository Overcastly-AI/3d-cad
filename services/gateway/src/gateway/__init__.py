"""Loft gateway service — auth, REST aggregation, WebSocket fan-out.

The single public entry point to the platform: the web app (and any other
client) talks only to this service (CLAUDE.md service boundaries).
"""
